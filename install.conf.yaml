- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.byobu:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.oh-my-zsh:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:

- shell:
    - [git submodule update --init --recursive, Installing submodules]

