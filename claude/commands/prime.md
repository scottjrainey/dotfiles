# Prime

Get oriented with the current project by reading foundational documentation.

## Run

1. Check for `.claude/commands/prime.md` in the project root - if found, follow that instead
2. Otherwise, read `README.md` and `CLAUDE.md` or `AGENTS.md` if they exist
3. Summarize your understanding of the project
